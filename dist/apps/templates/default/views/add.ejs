<!DOCTYPE html>
<html>
  <head>
    <title><%%= title %%></title>
    <%%- include('../../views/tile/head') %%>
  </head>
  <body>
<%%- include('../../views/tile/body-initial') %%>
  <%%- include('../../views/tile/navigation') %%>


<div class="wrap">
  <div class="container">
    <div class="row">
      <div class="column column-25">
          <%%- include('./sidebar') %%>
      </div>
      <div class="column column-75">

        <%%- hlp.form.start( "<%= name %>" , <%= name %> , { "id" : "<%= action %>" , "method" :"post" , "action" : "./" }); %%>
        <fieldset>
        <% fields.forEach(function (field) { %>
          <% if(field.name  === "updated_at" || field.name  === "created_at" || field.name  === "id" ) {
            return;
          } %>
          <div class="form-item">
            <label for="title"><%= field.name %></label>
            <%%- hlp.form.<%- field.tag %>("<%= field.name %>" , <%- field.attr %>); %%>
          </div>
        <% }); %>

        <%%- hlp.form.submit( "submit" , { "class" : "xhr-post" , "data-target" : "#<%= action %>" } ); %%>
        </fieldset>

        <%%- hlp.form.end(); %%>
      </div>
    </div>
  </div>
</div>

<%%- include('../../views/tile/footer') %%>
<%%- include('../../views/tile/scripts') %%>
<%%- include('../../views/tile/body-final') %%>

<script> 
</script>

  </body>
</html>

